# Either key enables LLM parsing. Without both, the rule engine handles everything.
# Gemini is free (500 req/day) — get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Anthropic is paid but higher quality — optional
ANTHROPIC_API_KEY=
PORT=4000
LLM_CACHE=1
